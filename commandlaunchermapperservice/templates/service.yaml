apiVersion: v1
kind: Service
metadata:
  name: {{ include "commandlaunchermapperservice.fullname" . }}
  labels:
    {{- include "commandlaunchermapperservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "commandlaunchermapperservice.selectorLabels" . | nindent 4 }}
