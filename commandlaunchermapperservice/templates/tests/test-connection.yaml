apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commandlaunchermapperservice.fullname" . }}-test-connection"
  labels:
    {{- include "commandlaunchermapperservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "commandlaunchermapperservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
