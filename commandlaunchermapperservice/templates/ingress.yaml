{{- if .Values.ingressRoute.enabled }}
{{- $fullName := include "commandlaunchermapperservice.fullname" . -}}
{{- $svcPort := .Values.service.port -}}
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "commandlaunchermapperservice.labels" . | nindent 4 }}
  {{- with .Values.ingressRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingressRoute.entry_points }}
  entryPoints:
    {{- toYaml . | nindent 4  }}
  {{- end }}
  routes:
  - kind: Rule
    match: {{ .Values.ingressRoute.match }}
    priority: {{ .Values.ingressRoute.priority }}  
    middlewares:
    - name: {{ include "commandlaunchermapperservice.fullname" . }}
    services:
    - name: {{ include "commandlaunchermapperservice.fullname" . }} 
      port: {{ .Values.service.port }}
{{- end }}